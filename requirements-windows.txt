# Windows environment for building and testing the desktop executable.
# Recommended interpreter:
#   C:\Users\bertr\miniforge3\envs\py311_llm\python.exe
#
# Usage:
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements-windows.txt
#
# This file is intentionally explicit so a fresh Windows env has everything
# needed to run readiness, launch the desktop app, and build the exe with PyInstaller.

setuptools>=68
wheel

transformers>=4.41.0
torch>=2.2.0
requests>=2.31.0
rapidfuzz>=3.9.0
Unidecode>=1.3.8
gliner>=0.2.10
huggingface-hub>=1.6.0

PySide6>=6.8.0
PyInstaller>=6.11.0

pytest>=9.0.2
pytest-qt>=4.4.0
ruff>=0.5.0

-e .
